{
  "config": {
    "step": {
      "user": {
        "title": "Etatherm Modbus",
        "description": "Connect to an Etatherm controller over Modbus TCP.",
        "data": {
          "host": "Host",
          "port": "Port",
          "modbus_addr": "Modbus unit ID",
          "unique_base": "Entity unique ID"
        },
        "data_description": {
          "unique_base": "If set, used as the climate unique ID instead of `{host}_{unit}`. Use the same value as the legacy YAML integration to keep existing entities."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This Etatherm controller is already configured"
    }
  }
}
